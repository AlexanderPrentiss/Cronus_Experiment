def bila(labels, epochs, minibatches, classes):

    for epoch in range(epochs):

        for batch in range(minibatches):

# Original Cronus Aggregator   
def robustFilter(labels, epsilon, k=0):
    """
    Docstring for robustFilter
    
    labels: 

    """



